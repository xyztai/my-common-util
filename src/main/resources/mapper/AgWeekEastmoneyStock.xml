<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.my.mapper.AgWeekEastmoneyStockMapper">

    <select id="getHs300List" resultType="net.my.pojo.HsStockPoJo">
        select stockCode, stockType, stockName
        from t_hs300
             -- where stockCode not like '30%'
             -- and stockCode not like '68%'
        order by 1
    </select>

    <select id="getMaxEastMoneyNode" resultType="java.lang.String">
        select max(date)
        from t_eastmoney_node_weekly
        where stockCode = #{stockCode}
          and `date` not like '99999%'
    </select>

    <insert id="saveEastMoneyDatas" parameterType="java.util.List">
        insert into t_eastmoney_node_weekly(`date`, `stockCode`, `infoRaw`)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.date}, #{item.stockCode}, #{item.infoRaw}
            )
        </foreach>
    </insert>

    <update id="updateEastMoneyDatas">
        update t_eastmoney_node_weekly
        set `date` = SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -11), ',', 1) ,
            `open` = SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -10), ',', 1) ,
            `last` = SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -9), ',', 1) ,
            `high` = SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -8), ',', 1) ,
            `low` = SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -7), ',', 1) ,
            `volume` = SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -6), ',', 1) ,
            `amount` = SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -5), ',', 1) ,
            `chg` = SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -3), ',', 1) ,
            -- SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -4), ',', 1),
            -- SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -3), ',', 1),
            -- SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -2), ',', 1),
            `exchangeRaw` = SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -1), ',', 1)
        where `open` is null
    </update>


    <insert id="genWeeklyData">
        <![CDATA[
        replace into t_eastmoney_node_weekly(`date`, `stockCode`, `open`, `last`, `high`, `low`, `volume`, `amount`, `chg` )
        with tmp_week as (
            select stockCode stockCode_w, max(date) date_w
            from t_eastmoney_node_weekly
            -- where `date` < '2026-04-10'
            group by stockCode
        )
        , tmp_1 as (
            select
                   rank() over (partition by ten.stockCode order by `date` ) date_rn
                   , rank() over (partition by ten.stockCode order by `date` desc) revert_date_rn
                   , ten.*
            from t_eastmoney_node ten, tmp_week tw
            where `date` > tw.date_w
            and `date` <= DATE_FORMAT( sysdate(), '%Y-%m-%d')
            and ten.stockCode = tw.stockCode_w
        )
        select max(case when revert_date_rn = 1 then ten.`date` else null end) date
               , ten.stockCode
               , max(case when date_rn = 1 then ten.`open` else null end) open
               , max(case when revert_date_rn = 1 then ten.`last` else null end) last
               , max(ten.high ) high
               , min(ten.low ) low
               , sum(ten.volume ) volume
               , round(sum(ten.amount ), 0) amount
               , round(max(case when revert_date_rn = 1 then ten.`last` else null end)/
                       (max(case when date_rn = 1 then ten.`last` else null end)) * (1 + 0.01 * max(case when date_rn = 1 then ten.`chg` else null end)) * 100
                 , 2) - 100 chg
        from tmp_1 ten
        group by ten.stockCode
        order by 1, 2
        ]]>
    </insert>


</mapper>