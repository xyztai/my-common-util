<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.my.mapper.AgEastmoneyIndexMapper">
    <select id="getStr" resultType="java.lang.String">
        select str
        from t_eastmoney_node_str
        where stockCode = #{stockCode}
        order by id desc
        limit 1
    </select>

    <insert id="saveIndexEastMoneyDatas" parameterType="java.util.List">
        replace into t_eastmoney_node_index(`date`, `stockCode`, `infoRaw`)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.date}, #{item.stockCode}, #{item.infoRaw}
            )
        </foreach>
    </insert>

    <update id="updateIndexEastMoneyDatas">
        update t_eastmoney_node_index
        set `date` = SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -11), ',', 1) ,
        `open` = SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -10), ',', 1) ,
        `last` = SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -9), ',', 1) ,
        `high` = SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -8), ',', 1) ,
        `low` = SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -7), ',', 1) ,
        `volume` = SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -6), ',', 1) ,
        `amount` = SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -5), ',', 1) ,
        `chg` = SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -3), ',', 1) ,
        -- SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -4), ',', 1),
        -- SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -3), ',', 1),
        -- SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -2), ',', 1),
        `exchangeRaw` = SUBSTRING_index(SUBSTRING_index(infoRaw, ',', -1), ',', 1)
        where `open` is null
    </update>

    <select id="xxxx" >
        select tt.date
                , chg
                , rank() over (order by chg) chg_rank
        from (
            select *
            from t_eastmoney_node_index
            order by 1 desc
            limit 250
        ) tt
        order by chg_rank
        limit 20
    </select>

    <select id="xxx1">
        select tt.date
        , chg, chg_chg_1
        , last_next_5
        , last_next_10
        , last_next_15
        , round((GREATEST(last_next_5, last_next_10, last_next_15)/`last` - 1 ) * 100, 1) greast_ratio
        , rank() over (order by chg) chg_rank
        from (
        select t.date, t.chg , t.`last`
        , lead(t.`chg`, 1) over (order by t.`date` ) chg_chg_1
        , lead(t.`last`, 5) over (order by t.`date` ) last_next_5
        , lead(t.`last`, 10) over (order by t.`date` ) last_next_10
        , lead(t.`last`, 15) over (order by t.`date` ) last_next_15
        , lead(t.date) over (order by t.`date` ) date_pre
        , lead(t.date, 1) over (order by t.`date` ) date_next_1
        , lead(t.date, 5) over (order by t.`date` ) date_next_5
        from t_eastmoney_node_index t
        where `date` < '2026'
        order by 1 desc
        limit 250
        ) tt
        where last_next_15 is not NULL
        order by chg_rank
        limit 20
    </select>

    <select id="xxxx2">
        <![CDATA[
            with tmp_res as (
                select tene.`date`
                        , lag(tene.date, 1) over (partition by tene.stockCode order by tene.`date` ) date_pre_1
                        , tene.stockCode
                        , t3.stockName
                        , tene.chg
                        , tene.`last`
                        , tene.volume
                        , lag(tene.`volume`, 1) over (partition by tene.stockCode order by tene.`date` ) volume_pre_1
                        , round(tene.volume/lag(tene.`volume`, 1) over (partition by tene.stockCode order by tene.`date` ) *100, 1) - 100 vol_ratio_1
                        , round(lead(tene.`last`, 10) over (partition by tene.stockCode order by tene.`date` )/tene.`last`*100, 1) - 100 chg_next_10
                        , lag(tene.`chg`, 1) over (partition by tene.stockCode order by tene.`date` ) chg_pre_1
                        , round(lead(tene.`high`, 1) over (partition by tene.stockCode order by tene.`date` )/tene.`last`*100, 1) - 100 high_next_1
                        , lead(tene.`chg`, 1) over (partition by tene.stockCode order by tene.`date` ) chg_next_1
                        , lead(tene.`chg`, 2) over (partition by tene.stockCode order by tene.`date` ) chg_next_2
                        , rank() over (partition by tene.date order by tene.chg desc, tene.stockCode ) chg_rank
                from t_eastmoney_node_etf tene , t_etf t3
                where 1 = 1
                -- and t3.stockName not like '0%'
                and tene.`date` not like '9999%'
                and tene.stockCode = concat(t3.stockType , '.', t3.stockCode )
                -- and tene.chg > 2
            )
            , tmp_target_res as (
                select t1.*
                from tmp_res t1
                where 1 = 1
                and t1.chg_rank <= 3
                and chg_pre_1 < 2
                -- and t1.chg > 2
            )
            , tmp_ans as (
	            select t1.*
	                   , GREATEST(chg_next_1, chg_next_1+chg_next_2) great
	            from tmp_target_res t1
	            where not exists(
	            	select 1
	            	from tmp_res t2
	            	where t2.chg_rank <= 3
	            	-- and t2.chg > 2
	            	and t1.date_pre_1 = t2.`date`
	            	and t1.stockCode  = t2.stockCode
	            )
	        )
	        select substr(date, 1, 4) 'year_'
	               , concat(round(sum(case when great > 0 then 1 else 0 end)/count(1)*100, 1), '%') win_cnt
	               , concat(round(sum(case when great > 1 then 1 else 0 end)/sum(case when abs(great) > 1 then 1 else 0 end)*100, 1), '%') win_1_cnt
	               , concat(round(sum(case when great > 2 then 1 else 0 end)/sum(case when abs(great) > 2 then 1 else 0 end)*100, 1), '%') win_2_cnt
	               , concat(round(sum(case when great > 3 then 1 else 0 end)/sum(case when abs(great) > 3 then 1 else 0 end)*100, 1), '%') win_3_cnt
	               , count(1) cnt
	               , sum(case when great > 1 then 1 else 0 end) win_1_cnt
	               , sum(case when great < -1 then 1 else 0 end) loss_1_cnt
	               , sum(case when great > 2 then 1 else 0 end) win_2_cnt
	               , sum(case when great < -2 then 1 else 0 end) loss_2_cnt
	               , sum(case when great > 3 then 1 else 0 end) win_3_cnt
	               , sum(case when great < -3 then 1 else 0 end) loss_3_cnt
	        from tmp_ans
	        where 1 = 1
	        and chg > 2
	        and vol_ratio_1 between 50 and 150
	        -- and great > 5
	        -- and great > 3
	        -- and date >= '2025-01-01'
	        group by substr(date, 1, 4)
	        order by 1 desc;
            -- order by 1 desc, chg_rank;
        ]]>
    </select>

    <select id="xxx3">
        <![CDATA[
            select
                    tt.date
                    , tt.date_pre_1
                    , tt.`last`
                    , tt.volume
                    , tt.volume_pre_1
                    , tt.chg
                    , tt.chg_pre_1
                    , tt.chg + tt.chg_pre_1
                    , round((GREATEST(
                        case when last_next_1 is null then -100 else last_next_1 end
                        , case when last_next_2 is null then -100 else last_next_2 end
                        , case when last_next_3 is null then -100 else last_next_3 end
                        , case when last_next_4 is null then -100 else last_next_4 end
                        , case when last_next_5 is null then -100 else last_next_5 end
                       -- , case when last_next_6 is null then -100 else last_next_6 end
                       -- , case when last_next_7 is null then -100 else last_next_7 end
                       -- , case when last_next_8 is null then -100 else last_next_8 end
                       -- , case when last_next_9 is null then -100 else last_next_9 end
                       -- , case when last_next_10 is null then -100 else last_next_10 end
                      )/`last` - 1 ) * 100, 1) greast_ratio
            from (
                select
                        t.date
                        , t.`last`
                        , t.volume
                        , t.chg
                        , lag(t.`volume`, 1) over (order by t.`date` ) volume_pre_1
                        , lag(t.`chg`, 1) over (order by t.`date` ) chg_pre_1
                        , lag(t.date, 1) over (order by t.`date` ) date_pre_1      -- 前一天日期
                        , lead(t.`last`, 1) over (order by t.`date` ) last_next_1
                        , lead(t.`last`, 2) over (order by t.`date` ) last_next_2
                        , lead(t.`last`, 3) over (order by t.`date` ) last_next_3
                        , lead(t.`last`, 4) over (order by t.`date` ) last_next_4
                        , lead(t.`last`, 5) over (order by t.`date` ) last_next_5
                        , lead(t.`last`, 6) over (order by t.`date` ) last_next_6
                        , lead(t.`last`, 7) over (order by t.`date` ) last_next_7
                        , lead(t.`last`, 8) over (order by t.`date` ) last_next_8
                        , lead(t.`last`, 9) over (order by t.`date` ) last_next_9
                        , lead(t.`last`, 10) over (order by t.`date` ) last_next_10
                from t_eastmoney_node_index t
                where `date` between DATE_FORMAT(DATE_ADD(STR_TO_DATE('2026-03-07', '%Y-%m-%d'), INTERVAL -250 DAY), '%Y-%m-%d') and '2026-03-07'
                order by chg -- case when chg_pre_1 is null then 1000 else chg_pre_1 end
                limit 20
            ) tt
            where last_next_1 is not null
            -- and volume > volume_pre_1
            order by chg;
          --  order by chg_pre_1 + chg;
        ]]>
    </select>

</mapper>