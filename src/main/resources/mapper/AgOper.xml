<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.my.mapper.DataCalcMapper">
    <resultMap id="queryOperResultMap" type="net.my.pojo.AgOper">
        <id column="time" property="time" />
        <result column="name" property="name" />
        <result column="oper_dir" property="operDir" />
        <result column="buy_oper" property="buyOper" />
        <result column="sell_oper" property="sellOper" />
        <result column="ratio_c" property="ratioC" />
    </resultMap>

    <resultMap id="queryParaMap" type="net.my.pojo.AgParaBO">
        <id column="id" property="id" />
        <result column="type" property="type" />
        <result column="name" property="name" />
        <result column="s_ratio" property="sRatio" />
        <result column="b_ratio" property="bRatio" />
    </resultMap>

    <resultMap id="queryCPMap" type="net.my.pojo.AgClosePriceBO">
        <id column="id" property="id" />
        <id column="time" property="time" />
        <result column="type" property="type" />
        <result column="name" property="name" />
        <result column="close_price" property="closePrice" />
    </resultMap>

    <resultMap id="queryExpectDataMap" type="net.my.pojo.AgExpectDataBO">
        <id column="id" property="id" />
        <result column="time" property="time" />
        <result column="type" property="type" />
        <result column="name" property="name" />
        <result column="close_price" property="closePrice" />
        <result column="b_action" property="bAction" />
        <result column="s_action" property="sAction" />
        <result column="b_ratio_ans" property="bRatioAns" />
        <result column="s_ratio_ans" property="sRatioAns" />
    </resultMap>

    <resultMap id="queryDataCalcMap" type="net.my.pojo.AgDataCalcBO">
        <id column="id" property="id" />
        <id column="time" property="time" />
        <result column="type" property="type" />
        <result column="name" property="name" />
        <result column="close_price" property="closePrice" />
        <result column="expma_5" property="expma5" />
        <result column="expma_37" property="expma37" />
        <result column="s_ratio_para" property="sRatioPara" />
        <result column="s_ratio" property="sRatio" />
        <result column="s_ratio_pre" property="sRatioPre" />
        <result column="b_ratio_para" property="bRatioPara" />
        <result column="b_ratio" property="bRatio" />
        <result column="b_ratio_pre" property="bRatioPre" />
    </resultMap>

    <resultMap id="queryIndustryCalcMap" type="net.my.pojo.AgIndustryCalcBO">
        <id column="id" property="id" />
        <id column="time" property="time" />
        <result column="type" property="type" />
        <result column="name" property="name" />
        <result column="expma_5" property="expma5" />
        <result column="expma_37" property="expma37" />
        <result column="s_ratio" property="sRatio" />
        <result column="b_ratio" property="bRatio" />
        <result column="close_price" property="closePrice" />
    </resultMap>

    <resultMap id="queryDataCntMap" type="net.my.pojo.AgDataCntBO">
        <id column="time" property="time" />
        <result column="cnt_cp" property="cntCp" />
        <result column="cnt_calc" property="cntCalc" />
        <result column="cnt_gap" property="cntGap" />
        <result column="names_miss" property="namesMiss" />
    </resultMap>




    <delete id="delIndustryCalc">
        delete from t_industry_calc
        where time = #{time}
        and   type = #{type}
    </delete>

    <insert id="saveIndustryCalc" parameterType="net.my.pojo.AgIndustryCalcBO">
        insert into t_industry_calc(time, type, name, expma_5, expma_37, s_ratio, b_ratio, close_price)
        values
        (#{time}, #{type}, #{name}, #{expma5}, #{expma37}, #{sRatio}, #{bRatio}, #{closePrice})
    </insert>

    <select id="getBuyInfo" resultType="string">
<![CDATA[
        select info
        from (
                 select
                     concat(t.time, ', ', t.name, ', ', t.type, ', bRatio= ', t.b_ratio) info
                      , row_number() over (partition by t.type order by t.b_ratio desc) b_rn
	                  , row_number() over (partition by t.type order by t.s_ratio desc) s_rn
	                  , row_number() over (partition by t.type order by t.time desc) time_rn
                 from t_industry_calc t
                 where t.create_time >= DATE_SUB(CURDATE(), INTERVAL 1 YEAR)
             ) tt
        where tt.b_rn = 1 and time_rn = 1
        order by 1
]]>
    </select>

    <select id="getHistoryBuyRatio" resultType="string">
<![CDATA[
        select info
        from (
                 select
                     concat(t.time, ', ', t.name, ', ', t.type, ', bRatio= ', t.b_ratio) info
                     , row_number() over (partition by t.type order by t.b_ratio desc) b_rn
	                 , row_number() over (partition by t.type order by t.s_ratio desc) s_rn
	                 , row_number() over (partition by t.type order by t.time desc) time_rn
                 from t_industry_calc t
                 where t.create_time >= DATE_SUB(CURDATE(), INTERVAL 1 YEAR)
             ) tt
        where tt.b_rn = 1
        order by 1
        ]]>
    </select>

    <select id="getLastestIndustryData" resultMap="queryIndustryCalcMap">
        select *
        from (
            select t.*
                   , row_number() over (partition by t.`type` order by 'time' desc) time_rn
            from t_industry_calc t
        ) tt
        where tt.time_rn = 1
    </select>


    <insert id="saveQqNodes" parameterType="java.util.List">
        INSERT IGNORE INTO t_qq_node(date, stockCode, open, last, high, low, volume, amount, exchangeRaw,
        clac_expma_5, clac_expma_10, clac_expma_20, clac_expma_37, clac_expma_60)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.date}, #{item.stockCode}, #{item.open}, #{item.last},
            #{item.high}, #{item.low}, #{item.volume}, #{item.amount}, #{item.exchangeRaw},
            #{item.expma5}, #{item.expma10}, #{item.expma20}, #{item.expma37}, #{item.expma60}
            )
        </foreach>
    </insert>


    <select id="getMaxQqNode" resultType="net.my.pojo.QqNode">
        select date, stockCode, open, last, high, low, volume, amount, exchangeRaw,
                clac_expma_5 expma5, clac_expma_10 expma10,
                clac_expma_20 expma20, clac_expma_37 expma37, clac_expma_60 expma60
        from t_qq_node
        where stockCode = #{stockCode}
        order by date desc
        limit 1
    </select>

    <select id="specialCare" resultType="net.my.pojo.SpecialCarePoJo">
        with tmp_a as (
            select stockCode, `date`, `last`, clac_expma_5, clac_expma_10, clac_expma_20, clac_expma_37, clac_expma_60
            , rank() over (PARTITION by stockCode order by `date` desc) rank_date
            , rank() over (PARTITION by stockCode order by clac_expma_10/clac_expma_5 desc) rank_10_5
            , rank() over (PARTITION by stockCode order by clac_expma_20/clac_expma_5 desc) rank_20_5
            , rank() over (PARTITION by stockCode order by clac_expma_37/clac_expma_5 desc) rank_37_5
            , rank() over (PARTITION by stockCode order by clac_expma_60/clac_expma_5 desc) rank_60_5
            from t_qq_node
            <if test="time != null and time != ''">
                where `date` <![CDATA[ <= ]]> #{time}
            </if>
        ),
        tmp_b as (
            select t1.*
            from tmp_a t1
            where rank_date = 1
            -- where `date` = '2025-11-21'
        ),
        tmp_c as (
            select stockCode
            , max(rank_10_5) rank_cnt
            from tmp_a
            group by stockCode
        )
        select t5.stockCode, t5.`date`, t5.`last`
            , round(ratio_b/4, 6) ratioB
            , round(ratio_s/4, 6) ratioS
        from (
            select t2.*
            , round(rank_10_5/t3.rank_cnt, 6) * 4 ratio_b
<!--            + round(rank_20_5/t3.rank_cnt, 6)-->
<!--            + round(rank_37_5/t3.rank_cnt, 6)-->
<!--            + round(rank_60_5/t3.rank_cnt, 6) ratio_b-->
            , round(1 - (1+rank_10_5)/t3.rank_cnt, 6) * 4 ratio_s
<!--            + round(1 - (1+rank_20_5)/t3.rank_cnt, 6)-->
<!--            + round(1 - (1+rank_37_5)/t3.rank_cnt, 6)-->
<!--            + round(1 - (1+rank_60_5)/t3.rank_cnt, 6) ratio_s-->
            from tmp_b t2, tmp_c t3
            where t2.stockCode = t3.stockCode
        ) t5
        order by ratioB, ratioS
    </select>





    <select id="getHs300List" resultType="net.my.pojo.HsStockPoJo">
        select stockCode, stockType, stockName
        from t_stock_code
        order by 1
    </select>




    <select id="queryEastmoneyVolSuddenlyRisedDeleted" resultType="net.my.pojo.SpecialCarePoJo2">
        <![CDATA[
        select concat('3_', t3.stockCode, '-', t3.stockName, '-', t1.`date`, '-', t1.chg ) stockCode, t1.date, t1.last
             , case when t1.end_buy_date = 1 then concat('慎重-Buy-', round(t1.to_buy, 3)) else concat('历史-', round(t1.to_buy, 3)) end ratioB
        from (
                 select t.`date` , t.stockCode , t.chg, t.volume , t.`last`, t.to_buy,
                        DATE_FORMAT(DATE_ADD(STR_TO_DATE(t.`date`, '%Y-%m-%d'), INTERVAL 14 DAY), '%Y-%m-%d') > DATE_FORMAT(SYSDATE(), '%Y-%m-%d') end_buy_date
                 from (
                          select t.`date` , t.stockCode , t.chg, t.volume , t.`open` , t.`last`, t.high , t.low
                               , lag(t.`date`, 5) over (partition by t.stockCode order by `date` desc) date_next
                          , least(lag(t.low , 1) over (partition by t.stockCode order by `date` desc)
                              , lag(t.low , 2) over (partition by t.stockCode order by `date` desc)
                              , lag(t.low , 3) over (partition by t.stockCode order by `date` desc)
                              , lag(t.low , 4) over (partition by t.stockCode order by `date` desc)
                              , lag(t.low , 5) over (partition by t.stockCode order by `date` desc)) to_buy -- T~T+5，共6个交易日的 low，T+6时B
                               , lead(t.volume) over (partition by t.stockCode order by `date` desc) pre_volume
                          from t_eastmoney_node t
                          where t.`date` > '2025-01-01'
                            -- and t.stockCode like '%600803'
                            and t.`date` not like '9999%'
                            and t.stockCode not like '%.688%'
                      ) t
                 where t.volume >= 3 * pre_volume
                   and t.chg >= 5
                   and t.high / t.`last` < 1.01
                   and t.`open` / t.low < 1.01
                   and date_next is not null
             ) t1, t_stock_code t3
        where t1.stockCode = concat(t3.stockType , '.', t3.stockCode )
              -- where t.high_gain_3 < 0.5 and  t.high_gain_4 < 0.5        and  t.high_gain_5 < 0.5 and t.high_gain_6 < 0.5 and  t.high_gain_7 < 0.5       and  t.high_gain_8 < 0.5 and t.high_gain_9 < 0.5 and  t.high_gain_10 < 0.5       and  t.high_gain_11 < 0.5 and t.high_gain_12 < 0.5 and  t.high_gain_13 < 0.5
        order by 2 desc
        ]]>
    </select>

</mapper>